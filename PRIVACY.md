# 隐私政策

**适用产品**：LightDict 轻词典（Chrome 扩展）
**最后更新**：2026-08-26

## 一句话版本

LightDict 不收集、不上传、不出售你的任何数据。它没有自己的服务器，没有账号体系，没有埋点统计。唯一离开你电脑的，是你主动选中要查的那段文字——它被直接发给你在设置里选定的第三方翻译 / 词典接口。

## 我们收集什么

**什么都不收集。** 本项目的开发者不运营任何服务器，收不到、也存不下你的任何信息。

## 哪些数据会发出去，发给谁

只有两种情况会发起网络请求，且都由你的操作直接触发：

1. **划词查询**：你选中一段文字并触发查询时，**这段选中的文字**会被发给你在设置页选定的引擎。
2. **整页翻译**：你点击工具栏的「整页翻译」按钮或按下快捷键时，**当前页面的正文文本**会被分批发给你选定的网页翻译引擎。源语言由引擎自己识别，不额外发请求去问，也不外发页面语言以外的任何信息。不点就一个字都不会外发。

这些请求**只携带待翻译的文本**，不包含页面地址、页面标题、Cookie、浏览器指纹或任何身份信息。所有请求都在后台 service worker 中发出，并显式设置 `credentials: 'omit'`。

**唯一的例外是微软翻译引擎**：Bing 的免注册翻译接口要求携带同一次访问种下的 `bing.com` cookie，因此该引擎（且仅该引擎）使用 `credentials: 'include'`。也就是说，使用微软翻译时，你的 `bing.com` cookie 会随请求一起发出。介意的话，在设置页换用其它引擎即可。

**可能收到你文本的第三方**（取决于你在设置页的选择）：

| 服务 | 域名 |
| --- | --- |
| Google 翻译 | `clients5.google.com`、`translate.googleapis.com` |
| 微软 / Bing 翻译 | `www.bing.com` |
| 有道翻译 / 有道词典 | `aidemo.youdao.com`、`dict.youdao.com` |
| 金山词霸 | `dict-mobile.iciba.com` |
| MyMemory | `api.mymemory.translated.net` |
| SimplyTranslate | `simplytranslate.org` |
| Free Dictionary | `api.dictionaryapi.dev` |
| Wiktionary | `en.wiktionary.org` |
| Datamuse | `api.datamuse.com` |

这些服务各有自己的隐私政策，本扩展无法代它们作出承诺。请求发出后如何被处理，取决于对应服务的条款。你可以在设置页随时更换引擎，从而选择把文本交给谁。

## 哪些数据留在你自己的电脑上

| 数据 | 存放位置 | 说明 |
| --- | --- | --- |
| 设置项（引擎选择、主题、延迟、嗓音等） | `chrome.storage.sync` | 若你在 Chrome 中登录并开启了同步，这些设置会随你的 Google 账号在设备间同步——这是 Chrome 的机制，本扩展不参与也接触不到传输过程 |
| 生词本 | `chrome.storage.local` | 仅存于本机，上限 2000 条，可在弹窗中导出或清空 |
| 查询结果缓存 | `chrome.storage.local` | 按「引擎组合 + 文本」缓存 7 天，上限 600 条，可在设置页清空 |
| 整页翻译译文缓存 | `chrome.storage.session` | 仅在一次浏览会话内有效，上限 3000 条，浏览器关闭即消失 |

以上数据全部由 Chrome 保管在本机，不会被上传到任何地方。卸载扩展时，Chrome 会一并删除它们。

## 权限说明

| 权限 | 用途 |
| --- | --- |
| `storage` | 保存上表中的设置、生词本与缓存 |
| `activeTab` | 整页翻译：仅在你点击工具栏图标或按下快捷键的那一刻，对当前标签页生效 |
| `host_permissions`（上表中的域名） | 向你选定的翻译 / 词典接口发起请求。列表被逐一列出而非使用通配符，因此扩展只能访问这些站点 |

内容脚本注入到 `<all_urls>` 是为了监听划词并渲染卡片；它在本地运行，不会因为被注入就向外发送任何东西。

## 儿童隐私

本扩展不面向 13 岁以下儿童，也不收集任何人的个人信息。

## 变更

本政策的任何修改都会更新本文件顶部的日期，并通过本仓库的提交历史公开可查。

## 联系

有疑问或发现与本文描述不符的行为，请在项目仓库提 Issue。
